"use client";import { signOut,useSession } from "next-auth/react";import { Bell,Building2,Clock3,LogOut,Search,UserCircle } from "lucide-react";import { roleNames,Role } from "@/lib/roles";import { useEffect,useState } from "react";
export default function Header(){const {data:session}=useSession();const [time,setTime]=useState("");useEffect(()=>{const update=()=>setTime(new Date().toLocaleString("ru-RU",{dateStyle:"short",timeStyle:"short"}));update();const id=setInterval(update,1000);return()=>clearInterval(id)},[]);const role=session?.user?.role as Role|undefined;return <header className="sticky top-0 z-50 flex h-18 items-center justify-between border-b border-slate-800 bg-[#0f172a]/95 px-6"><div className="flex items-center gap-6"><div className="flex items-center gap-3 rounded-2xl border border-slate-700 bg-slate-900 px-4 py-2"><Building2 size={22} className="text-yellow-400"/><div><p className="text-xs text-slate-400">Организация</p><span className="font-semibold text-white">ALTYN SAPA COMPANY</span></div></div><div className="relative"><Search size={18} className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400"/><input placeholder="Поиск..." className="w-[300px] rounded-2xl border border-slate-700 bg-slate-900 py-3 pl-11 text-white"/></div></div><div className="flex items-center gap-3"><div className="hidden items-center gap-2 rounded-xl border border-slate-700 bg-slate-900 px-4 py-2 lg:flex"><Clock3 size={18}/><span className="text-sm text-slate-300">{time}</span></div><button type="button" className="rounded-xl border border-slate-700 bg-slate-900 p-3"><Bell size={20}/></button><div className="flex items-center gap-3 rounded-2xl border border-slate-700 bg-slate-900 px-4 py-2"><UserCircle size={38} className="text-blue-400"/><div><p className="font-semibold text-white">{session?.user?.name??"Гость"}</p><p className="text-xs text-slate-400">{role?roleNames[role]:"Не авторизован"}</p></div></div>{session&&<button type="button" onClick={()=>signOut({callbackUrl:"/login"})} className="rounded-xl border border-red-500/30 bg-red-500/10 p-3 text-red-400"><LogOut size={20}/></button>}</div></header>}
