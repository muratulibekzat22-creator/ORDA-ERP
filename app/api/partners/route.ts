import { NextResponse } from "next/server";import { Role } from "@prisma/client";import { createPartner,getPartner,getPartners } from "@/lib/services/partner.service";import { prisma } from "@/lib/prisma";import { requirePermission } from "@/lib/server-auth";
export async function GET(){const auth=await requirePermission("partners");if(auth.response)return auth.response;if(auth.session!.user.role===Role.PARTNER){const partner=await prisma.partner.findUnique({where:{userId:Number(auth.session!.user.id)},select:{id:true}});if(!partner)return NextResponse.json({error:"Профиль партнёра не найден"},{status:404});const item=await getPartner(partner.id);return NextResponse.json(item?[item]:[]);}return NextResponse.json(await getPartners());}
export async function POST(request:Request){const auth=await requirePermission("partners");if(auth.response)return auth.response;if(auth.session!.user.role===Role.PARTNER)return NextResponse.json({error:"Недостаточно прав"},{status:403});const body=await request.json() as Record<string,unknown>;if(typeof body.name!=="string"||!body.name.trim())return NextResponse.json({error:"Укажите название партнёра"},{status:400});return NextResponse.json(await createPartner({name:body.name.trim(),phone:typeof body.phone==="string"?body.phone:undefined,city:typeof body.city==="string"?body.city:undefined,email:typeof body.email==="string"?body.email:undefined}),{status:201});}
