import { NextResponse } from "next/server";import { Role } from "@prisma/client";import { deletePartner,getPartner,updatePartner } from "@/lib/services/partner.service";import { prisma } from "@/lib/prisma";import { requirePermission } from "@/lib/server-auth";
async function own(id:number,userId:string){return !!await prisma.partner.findFirst({where:{id,userId:Number(userId)},select:{id:true}})}
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const auth=await requirePermission("partners");if(auth.response)return auth.response;const id=Number((await params).id);if(!Number.isInteger(id)||id<=0)return NextResponse.json({error:"Некорректный id"},{status:400});if(auth.session!.user.role===Role.PARTNER&&!await own(id,auth.session!.user.id))return NextResponse.json({error:"Партнёр не найден"},{status:404});const p=await getPartner(id);return p?NextResponse.json(p):NextResponse.json({error:"Партнёр не найден"},{status:404});}
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const auth=await requirePermission("partners");if(auth.response)return auth.response;if(auth.session!.user.role===Role.PARTNER)return NextResponse.json({error:"Недостаточно прав"},{status:403});const id=Number((await params).id);if(!Number.isInteger(id)||id<=0)return NextResponse.json({error:"Некорректный id"},{status:400});const b=await request.json() as Record<string,unknown>;if(typeof b.name!=="string"||!b.name.trim())return NextResponse.json({error:"Некорректные данные"},{status:400});try{return NextResponse.json(await updatePartner(id,{name:b.name.trim(),phone:typeof b.phone==="string"?b.phone:undefined,city:typeof b.city==="string"?b.city:undefined,email:typeof b.email==="string"?b.email:undefined,active:typeof b.active==="boolean"?b.active:undefined}));}catch{return NextResponse.json({error:"Партнёр не найден"},{status:404});}}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const auth=await requirePermission("partners");if(auth.response)return auth.response;if(auth.session!.user.role===Role.PARTNER)return NextResponse.json({error:"Недостаточно прав"},{status:403});const id=Number((await params).id);if(!Number.isInteger(id)||id<=0)return NextResponse.json({error:"Некорректный id"},{status:400});try{await deletePartner(id);return NextResponse.json({ok:true});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Ошибка удаления"},{status:409});}}
