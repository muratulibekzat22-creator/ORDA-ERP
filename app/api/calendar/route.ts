import { NextResponse } from "next/server";
import { createCalendarEvent,getCalendarData,moveCalendarEvent } from "@/lib/services/calendar.service";
import { requirePermission } from "@/lib/server-auth";
const toDate=(value:unknown)=>{const date=new Date(String(value));return Number.isNaN(date.getTime())?null:date};
export async function GET(request:Request){const auth=await requirePermission("calendar");if(auth.response)return auth.response;const id=Number(new URL(request.url).searchParams.get("assignedUserId"));return NextResponse.json(await getCalendarData({assignedUserId:Number.isInteger(id)&&id>0?id:undefined}));}
export async function POST(request:Request){const auth=await requirePermission("calendar");if(auth.response)return auth.response;try{const body=await request.json() as Record<string,unknown>,startDate=toDate(body.startDate),orderId=Number(body.orderId),assignedUserId=Number(body.assignedUserId);if(!startDate||!Number.isInteger(orderId)||!Number.isInteger(assignedUserId)||(body.sourceType!=="measurement"&&body.sourceType!=="production"))return NextResponse.json({error:"Некорректные данные"},{status:400});const result=await createCalendarEvent({sourceType:body.sourceType,orderId,startDate,assignedUserId,stage:typeof body.stage==="string"?body.stage:undefined,comment:typeof body.comment==="string"?body.comment:undefined,user:auth.session!.user.name??"Система"});return result?NextResponse.json(result,{status:201}):NextResponse.json({error:"Заказ не найден"},{status:404});}catch(error){console.error(error);return NextResponse.json({error:"Недопустимый исполнитель"},{status:409});}}
export async function PATCH(request:Request){const auth=await requirePermission("calendar");if(auth.response)return auth.response;const body=await request.json() as Record<string,unknown>,startDate=toDate(body.startDate),id=Number(body.id);if(!startDate||!Number.isInteger(id)||(body.sourceType!=="measurement"&&body.sourceType!=="production"))return NextResponse.json({error:"Некорректные данные"},{status:400});try{return NextResponse.json(await moveCalendarEvent({sourceType:body.sourceType,id,startDate,user:auth.session!.user.name??"Система"}));}catch{return NextResponse.json({error:"Событие не найдено"},{status:404});}}
