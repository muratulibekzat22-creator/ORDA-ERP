"use client";import { signIn } from "next-auth/react";import { useState } from "react";
export default function LoginPage(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");async function login(e:React.FormEvent){e.preventDefault();setError("");const result=await signIn("credentials",{email,password,redirect:false});if(result?.error)setError("Неверный email, пароль или пользователь отключён.");else window.location.assign("/");}return <main className="flex min-h-screen items-center justify-center bg-[#0B1120]"><form onSubmit={login} className="w-full max-w-md space-y-5 rounded-2xl border border-slate-700 bg-[#101827] p-8"><div className="text-center"><h1 className="text-4xl font-bold text-yellow-400">ORDA ERP</h1><p className="mt-2 text-slate-400">Вход в систему</p></div><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" className="w-full rounded-xl bg-slate-900 p-3 text-white"/><input required type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Пароль" className="w-full rounded-xl bg-slate-900 p-3 text-white"/>{error&&<p className="text-sm text-red-400">{error}</p>}<button className="w-full rounded-xl bg-blue-600 py-3 font-semibold text-white">Войти</button></form></main>}
